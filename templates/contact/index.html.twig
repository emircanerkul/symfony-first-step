{% extends 'base.html.twig' %}

{% block body %}
	<div class="container mt-4">
		<h1>Contact US</h1>

		{{ form_start(our_form) }}
		{{ form_widget(our_form) }}
		{{ form_end (our_form) }}
	</div>
{% endblock %}
